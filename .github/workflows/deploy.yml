name: Publish site

# Rebuilds and republishes the site when the front-end changes, without
# scraping. The nightly workflow publishes its own run, so this only needs
# to cover hand edits to the page itself.
on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "engine/export.py"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build data.json from the committed data
        run: |
          python3 engine/export.py --restore
          python3 engine/export.py --dump
        env:
          BUILD_ID: ${{ github.sha }}

      # Stamp the same id into the page, so it can tell whether the copy a
      # browser is holding matches what was published.
      - name: Stamp the build id into the page
        run: sed -i "s/__BUILD__/${GITHUB_SHA}/" site/index.html
      - name: Check the page structure
        run: python3 site/check_layout.py site/index.html

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - uses: actions/deploy-pages@v4
