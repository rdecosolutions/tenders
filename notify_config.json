{
  "_comment": "Which tender changes are worth a notification. Edit this file on GitHub and the next nightly run uses it. Empty list = no filter on that field.",

  "departments": [],
  "districts": [],
  "work_categories": [],
  "local_bodies": [],

  "min_amount": 0,
  "event_types": ["NEW", "CORRIGENDUM", "RETENDER", "DATE_EXTENSION", "CANCELLATION"],

  "_style": "digest = one summary notification. per_tender = one notification per match (only sensible with tight filters).",
  "style": "digest",
  "max_per_tender": 5
}
